[project]
name = "drips-multiplex-relations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dowker-complex",
    "drips-complex",
    "h5py>=3.13.0",
    "numpy>=2.1.3",
    "pandas>=2.2.3",
    "scikit-learn>=1.6.1",
    "tqdm>=4.67.1",
]

[tool.uv.sources]
myplot = { path = "../myplot", editable = true }
dowker-complex = { path = "../dowker_complex", editable = true }
drips-complex = { path = "../drips_complex", editable = true }

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "myplot",
]
